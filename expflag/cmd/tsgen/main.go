package main

import (
	"flag"
	"fmt"
	"os"
	"strings"

	"github.com/target/goalert/expflag"
)

func main() {
	out := flag.String("out", "", "output file")
	flag.Parse()

	if *out == "" {
		panic("missing -out")
	}

	fd, err := os.Create(*out)
	if err != nil {
		panic(err)
	}
	defer fd.Close()

	var names []string
	for _, f := range expflag.AllFlags() {
		names = append(names, fmt.Sprintf("'%s'", f))
	}

	fmt.Fprintf(fd, `// Code generated by expflag/cmd/tsgen DO NOT EDIT.

export type ExpFlag = %s
`, strings.Join(names, " | "))
}
