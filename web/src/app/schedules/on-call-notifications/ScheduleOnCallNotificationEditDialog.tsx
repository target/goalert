import React, { useState } from 'react'

interface ScheduleOnCallNotificationEditDialogProps {
  id: string
  scheduleID: string
}

export default function ScheduleOnCallNotificationEditDialog(
  p: ScheduleOnCallNotificationEditDialogProps,
): JSX.Element {
  return <React.Fragment />
}
